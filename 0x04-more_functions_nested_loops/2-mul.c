#include "main.h"

/**
 * mul - multiplies two integers
 * @c: store the result of the multiplication
 * @i: store the first number
 * @j: store the second number
 * Return: the result of the multiplication
 */
int mul(int i, int j)
{
	int c;

	c = i * j;

	return (c);
}