#include "main.h"

/**
 * add - adds two integers and returns the result
 * @i: int to be added to b
 * @j: int to be added to a
 * Return: sum of a and b
 */
int add(int i, int j)
{
	int c;

	c = i + j;

	return (c);
}
